# Réplica 12 — System One + rayos (HyperFrames + GSAP, una timeline pausada)

- Técnica: 4 clips en la pista 0 (toma 1 congelada `assets/shot1.jpg` t=0 → escena negra 1.67–4.1 → rayos 4.1–5.65 → toma 2 congelada `assets/shot2.jpg` de t=5.8). Subtítulos letra por letra con spans (`visibility`), palabra en curso rosa `#ee6f9f` y a blanco al terminar; "Public" + V de dos `<path>` con `strokeDashoffset`; piso de malla con `perspective` + `repeating-linear-gradient` de líneas duras; 160 rayos SVG con mulberry32 (semilla 20260917): los punteados corren hacia afuera y parpadean, los sólidos se estiran desde el centro y se apagan.
- Recetas: `count-up` de motion-anything se revisó pero usa rAF/IntersectionObserver; el contador se hizo con un tween GSAP sobre un objeto (`power2.out`, 0→100 en 1.25 s), determinista. Nada del registry.
- Se acerca: geometría y ritmo de los tres subtítulos, posición/tamaño del título y de "Public" con la V, tiempos de corte, curva del contador (21→60→89→98→100), rayos radiales con hueco al centro.
- No se acerca: en la referencia el número cuenta hasta **100x**, no 38x como decía el encargo (se siguió el clip); mis rayos son más densos y algo más brillantes que los del original (los suyos son más finos y con más punteado); la malla del piso en la referencia se ve más al fondo y la mía se apaga antes; el cursor negro antes de "is" es un bloque estático, en el original parpadea.
- Verificado sobre out.mp4: `start_time=0`, 195 frames, frame 0 con el entrevistado (`f0.png`); `blackdetect` marca 1.7–2.33 y 4.1–4.2 — la referencia también marca 1.7–2.3 (escena negra con malla tenue). Calificación: 4/5.
